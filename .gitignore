node_modules/
out/
dist/
release/
.vite/
*.log
.DS_Store

# Wrangler local state — miniflare simulator caches R2 blobs locally,
# easily 100MB+ which trips GitHub's file size limit.
.wrangler/
landing/.wrangler/

# Local secrets — never commit
.env
.env.local
.env.*.local
.env.production

sessions/

python-sidecar/.venv/
python-sidecar/__pycache__/
python-sidecar/build/
python-sidecar/dist/

RELEASE.md
